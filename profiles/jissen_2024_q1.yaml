display_name: "実践事例 2024年度 Q1"
document_type: jissen_practice_case
period: "2024_q1"

# Secret 名を文字列で指定（実行時に os.environ から取得）
input_folder_id_secret: DRIVE_FOLDER_JISSEN_2024_Q1
output_folder_id_secret: DRIVE_OUTPUT_JISSEN_2024_Q1

# 出力一覧シート名（同一スプレッドシート内に複数シートを置く前提）
output_sheet_name: "実践事例_2024Q1_出力一覧"

# 参加者マスターシート名（医院名標準化 + Gmail 下書きの TO 用ルックアップ）。
# 省略時は config.MASTER_SHEET_NAME（"参加者マスター"）にフォールバック。
# セミナーごとに別のマスターシートを使う場合のみ明示する。
master_sheet_name: "実践事例_2024Q1_参加者マスター"

# プロンプトテンプレート識別子（今回は全プロファイル共通）
prompt_template: jissen_practice_case
