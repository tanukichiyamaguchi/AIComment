.env
.env.*
__pycache__/
*.pyc
*.pyo
# NotoSansJP-Regular.ttf はリポジトリにコミットする（実行時DLをなくし CI/本番を安定化）。
# Bold は同一の variable font なので ensure_fonts() がローカルで複製生成する（DL不要）→ 引き続き無視。
assets/NotoSansJP-Bold.ttf
logs/
*.egg-info/

# Claude エージェントの一時 worktree（並列作業用）。リポジトリ管理外。
.claude/worktrees/

# Credential files
credentials.json
client_secret.json
client_secret*.json
token.json
service-account*.json
*-credentials.json
